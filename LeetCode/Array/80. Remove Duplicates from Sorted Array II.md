# 80. Remove Duplicates from Sorted Array II

## Problem Description
Given an integer array `nums` sorted in **non-decreasing order**, remove some duplicates **in-place** such that each unique element appears **at most twice**. The **relative order** of the elements should be kept the same.

Since it is impossible to change the length of the array in some languages, you must instead have the result be placed in the **first part** of the array `nums`. More formally, if there are `k` elements after removing the duplicates, then the first `k` elements of `nums` should hold the final result. It does not matter what you leave beyond the first `k` elements.

Return `k` after placing the final result in the first `k` slots of `nums`.

Do **not** allocate extra space for another array. You must do this by **modifying the input array in-place** with $\mathcal{O}(1)$ extra memory.

### Example 1
- **Input:** `nums = [1,1,1,2,2,3]`
- **Output:** `5`, `nums = [1,1,2,2,3,_]`
- **Explanation:** Your function should return `k = 5`, with the first five elements of `nums` being `1`, `1`, `2`, `2` and `3` respectively. It does not matter what you leave beyond the returned `k` (hence they are underscores).

### Example 2
- **Input:** `nums = [0,0,1,1,1,1,2,3,3]`
- **Output:** `7`, `nums = [0,0,1,1,2,3,3,_,_]`
- **Explanation:** Your function should return `k = 7`, with the first seven elements of `nums` being `0`, `0`, `1`, `1`, `2`, `3` and `3` respectively. It does not matter what you leave beyond the returned `k` (hence they are underscores).

---

## Approach 1: Auxiliary Array (Keeping Counts)

### Intuition
This approach uses an auxiliary array (`stor_arr`) to keep track of elements that appear at most twice. We iterate through the array, maintaining a counter of the current element's frequency. If the count is $\le 2$, we add the element to `stor_arr`. Finally, we copy the elements back to `nums` in-place and return the size.

### Implementation
```python
class Solution:
    def removeDuplicates(self, nums: list[int]) -> int: 
        stor_arr=[]
        count=1
        for i in range(len(nums)):
            if i==0 or nums[i]!=nums[i-1]:
                count=1
            else:
                count+=1
                
            if(count<=2):
                stor_arr.append(nums[i])
                
        nums[:] = stor_arr
        return len(nums)
```

### Complexity Analysis
- **Time Complexity:** $\mathcal{O}(N)$ where $N$ is the length of `nums`.
- **Space Complexity:** $\mathcal{O}(N)$ auxiliary space due to the additional `stor_arr` array.

---

## Approach 2: Two-Pointer (In-Place)

### Intuition
To solve the problem in $\mathcal{O}(1)$ auxiliary space, we use a two-pointer approach (`l` and `r`).
- The pointer `r` scans through the array to find identical elements and count their frequency.
- The pointer `l` is the write index for elements to be kept.
- For each unique element, we write it at most `min(2, count)` times at index `l`, then advance `l`.
- Finally, we return `l` which represents the new length of the valid portion of the array.

### Implementation
```python
class Solution:
    def removeDuplicates(self, nums: list[int]) -> int:
        l,r=0,0
        while r<len(nums):
            count=1
            while r+1<len(nums) and nums[r]==nums[r+1]:
                count+=1
                r+=1
            for i in range(min(2,count)):
                nums[l]=nums[r]
                l+=1
            r+=1
        
        return l
```

### Complexity Analysis
- **Time Complexity:** $\mathcal{O}(N)$ where $N$ is the length of `nums`. Each element is visited at most twice.
- **Space Complexity:** $\mathcal{O}(1)$ auxiliary space since no extra data structures are used.

---

## 💡 Key Takeaway / Pro-Tip
> [!TIP]
> This trick completely removes the need for extra memory. Whenever you encounter a LeetCode problem with a sorted array that mentions "in-place" or "$O(1)$ space", this two-pointer lookback method is almost always the hidden key!
